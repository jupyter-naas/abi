# diagrams/

Source files for architecture and system diagrams.

Store `.excalidraw`, `.drawio`, or `.svg` source files here. When a diagram is ready to publish, export it as a PNG into `../site/static/img/` and reference it from the relevant doc page.
