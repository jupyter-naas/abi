node_modules/
.next/
.open-next/
.wrangler/
.env
.env.local
*.tsbuildinfo
next-env.d.ts

# Generated by kickstart from config.example.yaml (the committed source of truth).
config/config.yaml
# The repo-root .gitignore ignores `config.*.yaml` (meant for the ABI
# config.local/remote overrides); un-ignore the template so it stays committed.
!config/config.example.yaml

# Runtime-written demo files (the app regenerates these on use).
data/globals/analytics_events.json
data/globals/pnl/
data/**/user_annotations/
