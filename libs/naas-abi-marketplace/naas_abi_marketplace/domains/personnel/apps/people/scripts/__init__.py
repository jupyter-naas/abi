"""People Search Python modules: datasets, payloads, SPARQL helpers, and export.

The app root keeps ``config_loader.py`` (beside ``config.yaml``) and ``api/``.
Everything else lives here, including ``export_people_from_graph`` for rebuilds.
"""
